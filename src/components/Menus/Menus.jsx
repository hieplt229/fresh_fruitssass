import React from "react";
import { ConstScroll, ProductsList } from "../../constant";

const Menus = () => {
  return (
    <section id={ConstScroll.products}>
      <p className="text-5xl text-primary text-center p-[300px]">Menus</p>
    </section>
  );
};

export default Menus;
